<%= form_for(@experiment_availability) do |f| %>
  <% if @experiment_availability.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@experiment_availability.errors.count, "error") %> prohibited this experiment_availability from being saved:</h2>
    <ul>
    <% @experiment_availability.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :experiment_id, "Experiment:" %><br />
    <%= f.select :experiment_id, @experiments.sort %>
  </div>
  <div class="field">
    <%= f.label :date, "Date:" %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :time, "Time (Optional):" %><br />
    <%= f.text_field :time %>
  </div>
  <div class="field">
    <%= f.check_box :is_available %>
    <%= f.label :is_available, "Experiment is available" %>
  </div>
  <div class="field">
    <%= f.label :comments, "Comment (Optional):" %><br />
    <%= f.text_area :comments, :rows => 5, :columns => 30 %>
  </div>
  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>
